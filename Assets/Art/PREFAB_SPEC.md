# 임시/최종 아트 프리팹 규격서 (로드맵 3·7)

> [`MapLoader`](../Scripts/Level/MapLoader.cs)의 **프리팹 슬롯**에 넣을 시각 프리팹을 만들 때의 사양.
> seam은 코드로 완성돼 있음(로드맵 2). **이 규격대로 프리팹만 만들어 슬롯에 드래그하면 끝** — 게임 코드 변경 0.
> 슬롯이 비어 있으면 지금처럼 색 사각형으로 폴백되므로, 만든 것부터 하나씩 채워도 됨.

## 공통 규칙 (모든 프리팹)

- **구성**: 루트 GameObject + `SpriteRenderer`(필수) + 선택적 `Animator`. **그게 전부.**
- **넣지 말 것**: Collider2D · Rigidbody2D · 게임 로직 스크립트(Mirror/Prism/GateDetector/PlayerController 등).
  충돌·물리·광학은 코드가 루트에 만드는 **실제 오브젝트**가 담당하고, 프리팹은 그 오브젝트의 `"visual"` 자식으로만 들어간다.
- **Pixels Per Unit(PPU) = 40** 으로 임포트(맵 규약 `cell_px=40`, 1셀=1유닛). → 텍스처 40px = 1유닛.
- **Pivot = Center(0.5, 0.5)**. 코드가 시각을 오브젝트 중심(localPosition 0)에 놓기 때문.
- **크기 = 최종 크기로 저작**(아래 표). 플레이스홀더의 얇은 막대/작은 사각형 스케일은 프리팹에 **적용되지 않는다**(거울·사다리 예외는 아래).
- **sortingOrder**: 코드가 `표의 base + 프리팹 내부 상대순서`로 세팅. 프리팹 안에서 여러 SpriteRenderer를 쓰면 상대 순서(0,1,2…)만 맞춰두면 됨.
- **애니메이션**: 코드는 Animator 파라미터를 건드리지 않는다. 임시 단계에선 **정적 스프라이트** 또는 **스스로 루프하는 idle 애니메이션**만 사용(거울 회전·게이트 개폐는 코드가 transform·색으로 처리).

## 제작 단위 (중요)

- **그림 그릴 때 쓰는 단위 = 픽셀(px). 임포트는 항상 `PPU 40`.** → `40px = 1칸`.
- 아래 표의 **`제작 px`가 실제 캔버스 크기**이고, `PPU 40`으로 임포트하면 `월드(unit)` 크기로 배치된다.
- 예) 40×40px → 1×1유닛(한 칸), 24×36px → 0.6×0.9유닛(플레이어).

### 화면 표시 배율 `artScale` (아트 파일과 무관)

- MapLoader의 **`artScale`**(기본 **2**)이 **화면에 그려지는 크기만** 배로 키운다. **아트 파일 크기·콜라이더·빛 판정은 전혀 바뀌지 않는다** → 퍼즐 동작 불변.
- 즉 **표의 `제작 px` 그대로 그리면 되고**, 게임에서 크게/작게 보이는 정도는 인스펙터의 `artScale` 숫자만 바꾸면 된다(1 = 원본 크기).
- ⚠️ 배율을 키우면 아트가 콜라이더보다 커진다(캐릭터·오브젝트는 의도된 것).
- **배율이 적용되지 않는 것**(크기가 게임 규칙에 묶여 있음):
  - **타일류(지형·벽·빛통과벽·발판)** — **칸에 정확히 맞춰진다**(아래 참고).
  - **게이트 문** — 개폐존 3칸에 맞춤.
  - **사다리** — 1칸 조각을 세로로 반복 배치(조각당 1칸 고정, 비율 유지).

### 타일은 "칸에 딱 맞춤" — 옆 칸과 연결되게

- **지형·벽·빛통과벽·발판**은 **그린 px와 무관하게 코드가 칸 크기에 정확히 맞춘다**(지형·벽 = 1×1칸, 발판 = 1×0.4칸).
- 덕분에 **옆에 있는 타일끼리 빈틈·겹침 없이 딱 맞물린다.** 크게 그리든 작게 그리든 결과는 같으므로 크기 걱정 없이 그려도 된다.
- 다만 **가로세로 비율은 맞춰서** 그릴 것(안 맞으면 눌리거나 늘어남): 지형·벽 = **정사각(1:1)**, 발판 = **가로:세로 = 5:2**(예: 40×16).
- 이음매가 보이지 않게 하려면 **타일 가장자리까지 꽉 채워** 그리고(투명 여백 금지), 텍스처 임포트에서 **Filter Mode: Point**(픽셀아트) 또는 여백 번짐 방지 설정을 쓸 것.

## 오브젝트별 규격

모두 **PPU 40**으로 임포트. `월드(unit)`은 **배율 1일 때의 기본 크기**이고, 실제 화면 크기는 여기에 `artScale`(기본 2)이 곱해진다.

| 슬롯(MapLoader 필드) | 한글 이름 | 제작 px | 월드(unit, 배율1) | 회전 | 스케일 | order |
|---|---|---|---|---|---|---|
| `terrainPrefab` | 지형(바닥) | **40 × 40** | 1 × 1 | — | — | 0 |
| `wallPrefab` | 벽(빛 차단) | **40 × 40** | 1 × 1 | — | — | 0 |
| `wallGlassPrefab` | 빛 통과 벽 | **40 × 40** | 1 × 1 | — | — | 0 |
| `platformPrefab` | 발판(빛 투과) | **40 × 16** | 1 × 0.4 | — | — | 1 |
| `platformSolidPrefab` | 발판(빛 차단) | **40 × 16** | 1 × 0.4 | — | — | 1 |
| `ladderPrefab` | 사다리(**1칸 조각**) | **24 × 40** | 0.6 × 1 (조각 1개) | — | **1칸 조각을 세로로 반복(코드)** | 1 |
| `lensPrefab` | 랜즈(빛나는 부분) | **32 × 32** | 0.8 × 0.8 | — | — | 5 |
| `torchPrefab` | 횃불(랜즈 거치대) | **40 × 60** | 1 × 1.5 | — | — | 4 |
| `mirrorPrefab` | 거울(돌아가는 반사면) | **44 × 10** | 1.1 × 0.2 | **코드가 −angle** | — | 5 |
| `mirrorFixedPrefab` | 고정 거울(반사면) | **44 × 10** | 1.1 × 0.2 | **코드가 −angle** | — | 5 |
| `prismPrefab` | 프리즘 | **36 × 36** | 0.9 × 0.9 | 정립(0°) | — | 5 |
| `gatePrefab` | 게이트 수광부 | **40 × 40** | 1 × 1 | — | — | 5 |
| `gateDoorPrefab` | 게이트 문(긴 하나의 아트) | **40 × 120** | **1 × 3 고정** | 가로형 존은 코드가 90° | **존에 맞춤(코드·배율 미적용)** | 5 |
| `decoyPrefab` | 디코이(가짜 광학) | **32 × 32** | 0.8 × 0.8 | — | — | 5 |
| `spawnPrefab` | 스폰 표식 | **24 × 24** | 0.6 × 0.6 | — | — | 8 |
| `playerPrefab` | 플레이어 | **24 × 36** | 0.6 × 0.9 | — | — | 9 |

### 부속(2조각) 오브젝트 — 아트가 나뉘는 것

- **랜즈 = `lensPrefab`(빛나는 부분) + `torchPrefab`(장착 횃불)**. 둘 다 **랜즈 위치에 중심 정렬**되고, 횃불은 랜즈 **뒤(order 4)**에 회전 없이 깔린다. 횃불 몸통을 아래로 뻗게 하려면 세로로 긴 캔버스에서 **랜즈 장착부가 위쪽**에 오게 그릴 것.
- **거울 = `mirrorPrefab`(돌아가는 반사면) 1개**. 반사면만 코드가 회전시킨다(거치대 없음).
  - **아트 기본각 보정 `mirrorArtAngleOffset`(기본 90°)**: 거울 아트를 세로로 그렸을 때 코드가 90° 보정해 준다. 방향이 반대면 인스펙터에서 −90으로.
  - **시작각 랜덤**: 맵 생성 시 회전 가능한 거울이 정답에서 ±45°(22.5°×2) 랜덤하게 틀어진 채 시작한다(`randomizeMirrors`). 고정 거울은 제외.
- **게이트 = `gatePrefab`(수광부, 별도 오브젝트) + `gateDoorPrefab`(문)**. 수광부와 문은 **서로 다른 위치의 별개 오브젝트**다(수광부=맵의 `gate.pos`, 문=`gate_open_zone`).
  - **수광부 = 40×40px(1칸)**. 프리팹은 원본 크기 그대로 쓰이고 화면 크기는 `artScale`이 결정한다. 빛 판정 콜라이더는 0.9×0.9로 고정이라 **아트 크기를 바꿔도 퍼즐 동작에는 영향 없음**.
  - **문 = 긴 하나의 아트**. 개폐존(전 스테이지 공통 **3칸**)을 덮는 단일 오브젝트로 생성된다. **세로로 긴 문 하나(40×120px, 1:3 비율)만 그리면** 코드가 존에 맞춰 넣는다(게임에선 1×3칸 고정, `artScale` 영향 없음).
  - **바닥 해치(가로형 존)는 코드가 같은 아트를 90° 눕혀** 3×1에 맞춘다 → **1×3짜리 하나로 세로문 3개 + 바닥해치 1개 전부 왜곡 없이 커버**. 그래서 문 아트는 **위/아래 구분이 크지 않은(90° 돌려도 어색하지 않은)** 디자인이 안전.
  - 코드가 원본 크기를 존에 맞추므로 정확히 1×3이 아니어도 되지만(자동 스케일), **1×3(40×120)으로 그리면 늘어남 없이 딱 맞음**.

## 주의가 필요한 3가지 (seam 계약)

1. **거울(반사면 `mirrorPrefab`)** — 코드가 회전시킨다(반사면 정렬). **세로 상태로 저작**하고(기본각 보정 `mirrorArtAngleOffset=90`) 회전은 코드에 맡길 것. 프리즘의 45°는 플레이스홀더 전용이라 프리즘 프리팹은 **정립**으로.
2. **사다리 — 1칸짜리 조각을 세로로 반복 배치** — **늘어나지 않는다.** 코드가 조각 프리팹을 사다리 길이만큼(h개) 세로로 쌓는다. 각 조각은 **세로 1칸에 맞춰지되 균일 배율이라 아트 비율이 그대로 유지**된다.
   - 그래서 **한 칸 분량의 사다리 조각(가로대 1~2개 + 양옆 기둥)** 만 그리면 된다. 위아래로 반복되므로 **조각의 위/아래 끝이 자연스럽게 이어지도록**(기둥이 끝까지 닿게) 그릴 것.
   - **가로 굵기는 아트의 가로:세로 비율이 결정**한다(24×40 → 0.6칸, 40×40 → 1칸). `artScale`은 적용되지 않는다(칸 격자에 맞춰야 하므로).
3. **게이트/문 색 피드백** — 열림/닫힘 시 코드가 **`visual`의 첫 SpriteRenderer 색을 바꾼다**(수광부=초록, 문=반투명). 색이 제대로 읽히려면 대표 스프라이트를 **중립/흰색 베이스**로 두는 게 안전(틴트가 곱해짐). 색 대신 애니메이터/자식으로 상태를 표현하고 싶으면 그래도 됨 — 그 경우 색 틴트는 무시된다(코드 null-safe).

- **랜즈 방향**: `lensPrefab`을 넣으면 플레이스홀더 방향 표시 점은 자동 생략된다. **단 코드는 랜즈를 빔 방향으로 회전시키지 않으므로**(현재), 방향 비의존 **대칭 아트**를 권장. 방향성 아트가 꼭 필요하면 랜즈에 회전을 주입하는 소규모 코드 변경이 필요(요청 시 추가).
- **스폰 표식**: 최종 빌드에선 대개 불필요 → `spawnPrefab`은 비워두면 표식이 사라진다(색 사각형도 안 나옴은 아님 — 비우면 흰 사각형 폴백이 남으니, 표식을 없애려면 투명 1px 스프라이트 프리팹을 넣거나 별도 처리).

## Unity에서 만들고 배정하는 순서

1. 스프라이트 PNG를 `Assets/Art/Sprites/` 에 넣고, 각 텍스처를 선택 → Inspector에서 **Sprite (2D and UI)**, **Pixels Per Unit = 40**, **Pivot = Center**, (픽셀아트면) **Filter Mode = Point / Compression = None**.
2. 빈 GameObject에 `SpriteRenderer` 추가 → 스프라이트 지정 → `Assets/Art/Prefabs/` 로 드래그해 프리팹화. (콜라이더·스크립트 넣지 말 것)
3. **SampleScene**에서 `MapLoader` 컴포넌트 선택 → **프리팹 슬롯**에 해당 프리팹을 드래그.
4. ▶ Play(또는 컴포넌트 우클릭 Build)로 확인. 슬롯이 빈 것은 색 사각형으로 남는다.
5. 만족하면 **씬 저장**(슬롯은 씬의 MapLoader에 직렬화됨) + 스프라이트/프리팹/씬을 커밋.

> ⚠️ 새로 추가한 애셋의 `.meta`는 Unity가 임포트 시 생성한다 → 반드시 **에디터로 한 번 연 뒤** `.meta`까지 함께 커밋할 것(안 그러면 다른 PC에서 참조가 깨짐).
